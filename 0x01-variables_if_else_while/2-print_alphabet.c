#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char alphabet = 'a';

	while (alphabet < 'z')
	{
		putchar(alphabet);
		alphabeth++;
	{
	return (0);

}

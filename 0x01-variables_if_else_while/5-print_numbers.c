#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int numbers = 0;

	while (numbers < 10)
	{
		printf(numbers);
		number++;
	}
	printf("\n");
	return (0);


}
